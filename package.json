{
  "name": "currents-pnpm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress": "currents run --parallel --record --key xxx --ci-build-id `date +%s`"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@currents/cli": "^2.0.0",
    "cypress": "^9.6.0"
  }
}
